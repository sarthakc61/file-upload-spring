server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/file_upload
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 10MB  #File related Config

#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect


